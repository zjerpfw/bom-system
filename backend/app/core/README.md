# core 模块说明

本目录用于存放系统核心能力。

## 文件说明

- `config.py`：读取 `.env` 中的配置项。
- `database.py`：创建异步数据库引擎、会话依赖和初始化函数，并为 SQLite 补充轻量表结构迁移。

所有密钥和连接配置都必须来自 `.env`，不得在代码中硬编码。

如果 `.env` 中配置了 `API_KEY`，所有 `/api/*` 请求都需要在 Header 中传入 `X-API-Key`；未配置时默认跳过鉴权，便于本地开发。
